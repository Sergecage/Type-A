import {encoded, translations} from './data.js'

const translateIdLibrary = (source, target) => {
    const result = source.reduce(index, value => element.index == target.value);
    console.log(result);
}
translateIdLibrary(encoded,translations);